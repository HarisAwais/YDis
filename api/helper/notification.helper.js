const sendNotification = (recipientId, message) => {
    // Replace this with your actual notification logic
    console.log(`Sending notification to recipient ${recipientId}: ${message}`);
  };
  
  module.exports = sendNotification;